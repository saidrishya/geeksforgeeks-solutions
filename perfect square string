/*
You are given a string S. Your task is to determine if sum of ASCII values of all character results in a perfect square or not.

Input:
The first line of the input contains a single integer T, denoting the number of test cases. Then T test cases follow. Each testcase has a 
string S

Output:
Print 1 if the resulting sum is a perfect square, else print 0.

Constraints:
1<=T<=100
1<=|S|<=1000

Example:

Input:
4
1Qy
abcd
efgh
d

Output:
0
0
0
1
*/

#include <bits/stdc++.h>
using namespace std;

int main() {
	int t; cin>>t;
	while(t--){
	    string s; cin>>s;
	    int sum=0;
	    for(auto i:s){
	        sum =  sum + (int)i;
	    }
	    if(ceil(sqrt(sum)) == floor(sqrt(sum))) cout<<1<<'\n';
	    else cout<<0<<'\n';
	}
	return 0;
}
